jobs: build: runs-on: ubuntu-latest steps: - uses: actions/checkout@v4 - name: Setup Node uses: actions/setup-node@v4 with: node-version: 18 - name: Install run: | if [ -f package-lock.json ]; then npm ci; else echo "Không có package-lock.json"; fi - name: Lint / Test placeholder run: echo "Chưa có bài kiểm tra nào được cấu hình" env: OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }} GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
